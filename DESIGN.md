# DESIGN.md

## 产品灵魂

**产品**：个人账号密码存储管理系统（密码管家）

**意象锚点**：英式私人书房 / 旧时金库 / 绅士手边的皮面笔记本。
不张扬的、值得托付的——像把钥匙交给一位值得信任的老管家。

## 校验第一直觉

最显而易见的方案是「科技蓝 + 圆角卡片 + 锁图标」，这是密码管理器行业的默认审美（1Password、B 端 SaaS 都在用）。
对个人密码管家来说，**用户要的不是「科技感」，是「安心感」**——一个自己也想每天打开的地方。
所以绕开：科技蓝、紫色渐变、emoji 装饰、夸张的发光阴影、玻璃拟态。

## 视觉策略

**风格关键词**：书房质感（library / leather-bound / soft warm light）

- 暗色 + 暖光暗调，像深夜书桌旁点着一盏台灯
- 卡片有"纸"和"皮"的温度感，不用纯白
- 文字优先用衬线字体（中英文皆然），像手写笔记
- 图标用线性、极简、不喧宾夺主
- 整体节奏：留白、安静、克制

## 配色方案

| Token | 值 | 意象 |
|------|------|------|
| 背景（深） | `oklch(0.18 0.02 60)` | 深夜书房的木桌 |
| 卡片（深） | `oklch(0.23 0.025 65)` | 皮面笔记本的封面 |
| 主文字 | `oklch(0.92 0.02 80)` | 暖光下的米色纸张 |
| 次文字 | `oklch(0.65 0.02 70)` | 褪色的老墨水 |
| 主色 | `oklch(0.55 0.09 145)` | 墨绿/深苔藓，书脊的布面 |
| 强调 | `oklch(0.75 0.12 70)` | 老式黄铜台灯的暖光 |
| 危险 | `oklch(0.6 0.18 28)` | 暗红，像火漆封缄 |
| 边框 | `oklch(0.32 0.02 65)` | 皮革缝线 |

> 默认走暗色模式（密码管理类工具用户的偏好），不提供亮色主题切换（保持克制）。

## 字体排版

- **中文**：`Noto Serif SC`（思源宋体）—— 衬线、有书卷气
- **英文/数字**：`Cormorant Garamond`（密码标题等需要 elegant 的场合）+ `JetBrains Mono`（密码字段等需要等宽的场合）
- 节奏：标题用衬线大字（32-48px），正文 14-16px，标签 12-13px 字距略宽
- 避免：太粗的黑体、过度紧凑的行距

## 动效与交互

- 进入页面：淡入 + 轻微上浮（20px → 0，400ms，ease-out）
- 密码显示/隐藏：眼睛图标淡入淡出
- 复制成功：黄铜色 toast 滑入，2 秒后淡出
- 删除：先 modal 二次确认（与 auth-config 规范一致），不用行内删除
- 列表项 hover：左侧出现细的金棕色边线，不改变背景
- 缓动：`cubic-bezier(0.4, 0, 0.2, 1)`，避免弹跳

## 页面结构

### 1. 登录 / 注册页
- 居中卡片，宽 ~420px
- 上方：应用图标（icon_url）+ 应用名"密码管家"
- 表单：邮箱 + 密码（注册页多一个确认密码）
- 密码框带眼睛图标
- 登录按钮：墨绿主色，hover 时稍微提亮
- 登录/注册切换：底部细字链接

### 2. 主页（vault）
- 顶部：左侧 logo + 应用名，右侧搜索框 + 用户菜单（登出）
- 左侧栏：分类列表（默认 / 社交 / 工作 / 金融 / 其他），含"+"新建分类
- 右侧主区：
  - 工具栏：当前分类下条目数量 + "新增条目"按钮
  - 条目列表：每条目一行，标题 + 用户名 + 右侧操作（编辑/删除/复制密码）
  - 空状态：暖光的占位插画（"您还没有保存任何密码"）
- 模态框：新增/编辑条目

### 3. 用户菜单
- 头像（首字母） + 邮箱
- 下拉：登出

## 设计禁忌

- ❌ 科技蓝/紫渐变
- ❌ 玻璃拟态 / 模糊背景
- ❌ emoji 装饰
- ❌ 圆角超过 12px 的"卡片"
- ❌ 亮色模式切换（保持单一暗色）
- ❌ 用 emoji 代替图标（用 lucide）
- ❌ 任何动效里的弹跳
- ❌ 「您的数据已被加密保护」类俗套文案
